<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
    <metadata_license>MIT</metadata_license>

    <id>${APP_ID}</id>
    <name>${APP_FRIENDLY_NAME}</name>
    <summary>${APP_SHORT_SUMMARY}</summary>
    <developer_name>${PUBLISHER_NAME}</developer_name>
    <url type="homepage">${PUBLISHER_LINK_URL}</url>
    <project_license>${APP_LICENSE_ID}</project_license>
    <content_rating type="oars-1.1" />

    <launchable type="desktop-id">${APP_ID}.desktop</launchable>

    <description>
        <p>Hello World demonstrates all the major features of building deployable packages with PupNet.
        It can be built for all deployment package kinds, including AppImage, Flatpak, DEB and RPM formats on Linux,
        and as a Setup file on Windows. It provides an example of using desktop and AppStream metadata files,
        as well as icons and a post-publish script.</p>

        <p>You will notice, for example, that this AppStream metadata file and associated desktop file make</p>
        <p>use of macro variables for a high degree of content automation, so that information need only be
        specified once in the primary configuration.</p>

        <p>On execution, the Hello World program displays command arguments and assembly information. The assembly
        version confirms that version information may be provided in the deployment configuration, and passed through
        the build system, to the application.</p>
    </description>

    <!-- Freedesktop Categories -->
    <categories>
        <category>${PRIME_CATEGORY}</category>
    </categories>

    <keywords>
        <keyword>development</keyword>
        <keyword>programming</keyword>
    </keywords>

    <screenshots>
        <screenshot type="default">
            <image>https://i.postimg.cc/0jc8xxxC/Hello-Computer.png</image>
        </screenshot>
    </screenshots>

    <releases>
        <release version="${APP_VERSION}" date="${BUILD_DATE}">
            <description><p>The latest release.</p></description>
        </release>
    </releases>

</component>
