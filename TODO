Add screenshot to metainfo.xml
Update windows screenshot in readme
Output tool version in --version screen with --verbose??
Don't show AppImage version if not packaged


Add multiple --kind values to arguments
